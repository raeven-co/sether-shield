{
  "name": "sether-shield",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Sether Shield — a browser extension that catches personal data in your prompt before it reaches ChatGPT, Claude, or Gemini. 100% local.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node test/detector.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@raeven-co/sether": "^0.5.0"
  }
}
